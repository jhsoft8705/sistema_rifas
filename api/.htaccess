#modificar para el correcto funcionamiento de la API
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /CONTROL_ASISTENCIA_CAFED/api/
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [QSA,L]
</IfModule>
